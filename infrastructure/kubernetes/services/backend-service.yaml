apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: caption-gen
  labels:
    app: backend-service
spec:
  type: ClusterIP  # Internal service only
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: backend-service

