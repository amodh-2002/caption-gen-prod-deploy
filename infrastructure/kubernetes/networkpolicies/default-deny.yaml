apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: caption-gen
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
  # This denies all traffic by default
  # Other NetworkPolicies will allow specific traffic

